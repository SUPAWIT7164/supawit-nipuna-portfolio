/**
 * Static GitHub copy kept as documentation/fallback reference.
 * Live profile + repositories are fetched in `lib/github.ts`.
 */
export const githubContributionSummary =
  "โฟกัสที่การส่งมอบฟีเจอร์ที่อ่านง่าย ดูแลต่อได้ และพร้อมใช้งานจริง ทั้งฝั่ง Frontend, Backend และการเชื่อมต่อระบบภายนอก";
